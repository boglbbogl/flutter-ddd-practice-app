import 'package:flutter/material.dart';

class VelogStateGetX extends StatelessWidget {
  const VelogStateGetX({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
