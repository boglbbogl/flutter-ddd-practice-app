import 'package:flutter/material.dart';

class ApiNaverRomanizationMainPage extends StatelessWidget {
  const ApiNaverRomanizationMainPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
